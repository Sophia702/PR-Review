# App name must be globally unique on Fly — change this before `fly apps create`.
app = "pr-review-dashboard"
primary_region = "iad"

[build]

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
